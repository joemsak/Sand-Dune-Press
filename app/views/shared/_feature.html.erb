<% if @header_comic %>
<section id="feature">
	<%= image_tag @header_comic.header_image.url, :class => 'header-image' %>
	<% if (@header_comic.created_at.to_i - Time.now.to_i) > 1209600  %>
		<%= image_tag 'newcomic', :class => 'newcomic' %>
	<% end -%>
</section>
<% end %>